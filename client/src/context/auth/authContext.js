// Import React package and hooks
import { createContext } from 'react';

// Initialize new context
const authContext = createContext();

// Export context instance to use in application
export default authContext;

