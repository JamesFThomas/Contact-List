// Import React and deconstruct createContext hook from package
import { createContext } from 'react';

// Initialize new context for user contacts
const contactContext = createContext();

// Export new context for user contacts
export default contactContext;